using FFChessShared;

namespace Server.Chess;

public class GameManager
{
	public void Move(ChessMove move)
	{
		/*TODO var piece = Board[move.From.X, move.From.Y];
		Board[move.To.X, move.To.Y] = piece;
		Board[move.From.X, move.From.Y] = 0;*/
	}
}
